#include <stdio.h>
#include "main.h"

/**
 * _abs - computes abstract value of a num
 * @n: num to compute
 *
 * Return: 0
 */

int _abs(int)
{
	int a;

	if (n >= 0)
		a = n;
	else
		a = n * -1;
	return (a);
}
